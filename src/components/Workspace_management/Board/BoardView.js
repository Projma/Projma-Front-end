import * as React from "react";
import "./BoardView.css";

const BoardView = (props) => {
  return (
    <div className="view">
      <p className="board-title">اسم</p>
    </div>
  );
};

export default BoardView;
